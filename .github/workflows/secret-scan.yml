name: secret-scan

on:
  pull_request:
  push:
    branches: [main]
  schedule:
    - cron: "0 6 * * 1"

permissions:
  contents: read

concurrency:
  group: secret-scan-${{ github.ref }}
  cancel-in-progress: true

jobs:
  gitleaks:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout full history
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          fetch-depth: 0

      - name: Run gitleaks
        shell: bash
        run: |
          set -euo pipefail
          docker run --rm -v "$PWD:/repo" \
            zricethezav/gitleaks@sha256:b5918eb91b8d2473cec722f066abb4352e4ffdc4ec9f4283ec143aba9ec9ebc4 \
            git /repo --config=/repo/.gitleaks.toml --redact --verbose --exit-code 1

  deny-list:
    name: deny-list leak scan
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0

      # Project-specific topology / identity-linking deny-list, scanned across the
      # working tree. Complements gitleaks (secrets, git history) — see
      # scripts/leak-scan.sh for the classes it enforces.
      - name: Deny-list leak scan
        run: bash scripts/leak-scan.sh
